# 프로젝트 작업 규칙

## 데이터 보존

1. `data/raw/`의 파일을 수정하거나 덮어쓰지 않는다.
2. 실제 파일의 열 이름, 자료형, 행 수를 확인한 뒤 코드를 작성한다.
3. 캠페인 번호, 기간, 대상 상품을 코드에 고정하지 않고 원본 테이블에서 조회한다.

## 분석 설계

1. 처치집단은 선택 캠페인을 받았고 같은 기간에 겹치는 다른 캠페인을 받지 않은 가구로 정의한다.
2. 대조집단은 선택 캠페인을 받지 않았고 같은 기간에 겹치는 다른 캠페인도 받지 않은 가구로 정의한다.
3. 발행 전 특성에는 캠페인 시작일 이전 정보만 사용한다.
4. 캠페인 기간의 구매, 쿠폰 사용과 기타 사후 정보는 성향점수 입력변수로 사용하지 않는다.
5. 주요 결과는 캠페인 대상 상품의 구매 여부, 구매금액과 구매수량이다. 전체 구매 결과는 보조지표로 구분한다.
6. 기간이 다른 캠페인을 비교할 때 실제 기간 결과와 일평균 또는 첫 30일 기준 결과를 함께 제공한다.

## 품질과 해석

1. 분석표의 가구 수와 구매금액을 원본에서 독립적으로 재계산해 대조한다.
2. 단순 집단 차이를 인과효과라고 표현하지 않는다.
3. 성향점수 분포, 공통지지영역, 매칭률과 매칭 전후 표준화 평균차이를 효과보다 먼저 확인한다.
4. 표본 부족, 공통영역 부족 또는 큰 잔여 불균형이 있으면 효과를 확정하지 않고 상태와 이유를 반환한다.
5. 모든 결과에 캠페인, 기간, 관측 단위, 계산 기준과 데이터 단위를 표시한다.
6. 관찰된 사실, 모델 추정치와 사용자가 입력한 수익성 가정을 구분한다.
7. 후보 시나리오 중 가장 높은 결과를 전역 최적 쿠폰이라고 표현하지 않는다.

## 파일 관리

1. 중간 확인 결과는 터미널에 우선 출력하고 불필요한 파일을 만들지 않는다.
2. 캠페인별 최종 결과는 `analysis_data.csv`, `matched_data.csv`, `results.json`, `report.html` 네 파일을 기본으로 한다.
3. 다른 캠페인의 결과를 덮어쓰지 않도록 `outputs/campaign_{id}/`에 저장한다.
4. 실행 코드와 분석 조건을 남겨 같은 입력으로 재현할 수 있게 한다.
5. 분석 재실행은 캠페인, 발행 전 기간, 결과변수 또는 매칭 조건이 바뀔 때만 수행한다. 마진·비용 입력 변경은 수익성 계산만 다시 수행한다.
